/******************************************************************************

                              Online C++ Compiler.
               Code, Compile, Run and Debug C++ program online.
Write your code in this editor and press "Run" button to compile and execute it.

*******************************************************************************/

#include <iostream>
#include <cmath>
#include <algorithm>
using namespace std;

int main()
{
    int ok,tw=0,P,x,n;
    string sts,sts2,ns,xs;
    getline(cin, sts);
    for(int i = 0;i<sts.length();i++){
        if(ok == 0 && sts[i] != ' '){
            ns = ns + sts[i];
        }else if(sts[i] == ' '){
            ok++;
        }else{
            xs = xs + sts[i];
        }
    }
        x = stoi(xs);
        n = stoi(ns);
    for(int j=0;j<n;j++){
        cin >> P;
        tw = tw + P;
    }
        getline(cin, sts2);
        cout << sts << " " << sts2;
        return 0;
}
